# Fonts

The **BRASA** landing title uses **Bricolage Grotesque** from Google Fonts, loaded via `next/font/google` in `app/layout.tsx` (no files needed here).

You can still add other static assets under `public/fonts/` if needed.
